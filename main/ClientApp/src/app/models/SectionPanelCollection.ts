import { Panel } from './Panel';
/**
 * A section that contains a one or more panels.
 * Panels could be face, plan, hip or cross arms
 */
class SectionPanelCollection {
    /**
     * Adds a panel at the specified height
     * Does not check for collisions between panels
     * @param panel
     * @param height
     */
    addPanel(panel: Panel, height: number) {
    }
    /**
     * Returns panels by type
     */
    getPanel() {
    }
}
